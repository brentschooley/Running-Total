<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="http://infragistics.com/Controls"
    xmlns:shared="using:Infragistics"
    xmlns:local="using:Infragistics.Controls.Charts"
    xmlns:ig="using:Infragistics.Controls">

    <!-- *********************************  COLORS  ********************************* -->
    <Color x:Key="Color_000">#FF282828</Color>
    <Color x:Key="Color_001">#FF3F3F3F</Color>
    <Color x:Key="Color_002">#FF565656</Color>
    <Color x:Key="Color_003">#FF858585</Color>
    <Color x:Key="Color_004">#FFB9B9B9</Color>
    <Color x:Key="Color_005">#FFD7D7D7</Color>
    <Color x:Key="Color_006">#FFE7E7E7</Color>
    <Color x:Key="Color_007">#FFF4F4F4</Color>
    <Color x:Key="Color_008">#FFF9F9F9</Color>
    <Color x:Key="Color_009">#FFFFFFFF</Color>
    <Color x:Key="Color_010">#FFB2E1EF</Color>
    <Color x:Key="Color_011">#FF00AADE</Color>
    <Color x:Key="Color_012">#FFF55E7F</Color>
    <Color x:Key="Color_013">#FFD0284C</Color>
    <Color x:Key="Color_014">#00FFFFFF</Color>
    <Color x:Key="Color_015">#66E2E2E2</Color>
    <Color x:Key="Color_016">#A5FFFFFF</Color>
    <Color x:Key="Color_0111">#FF006481</Color>
    <Color x:Key="Color_022">#FF686868</Color>
    <Color x:Key="Color_0221">#FF373737</Color>
    <Color x:Key="Color_023">#FF8A9B0F</Color>
    <Color x:Key="Color_0231">#FF3E4700</Color>
    <Color x:Key="Color_024">#FFF14D0F</Color>
    <Color x:Key="Color_0241">#FF8D2E00</Color>
    <Color x:Key="Color_025">#FF81106B</Color>
    <Color x:Key="Color_0251">#FF410135</Color>
    <Color x:Key="Color_026">#FFFCA910</Color>
    <Color x:Key="Color_0261">#FF8D4902</Color>
    <Color x:Key="Color_027">#FF037A54</Color>
    <Color x:Key="Color_0271">#FF003F2A</Color>
    <Color x:Key="Color_028">#FF154D85</Color>
    <Color x:Key="Color_0281">#FF02284D</Color>
    <Color x:Key="Color_029">#FF543511</Color>
    <Color x:Key="Color_0291">#FF211303</Color>
    <Color x:Key="Color_030">#FF89806D</Color>
    <Color x:Key="Color_0301">#FF393225</Color>
    <Color x:Key="Color_031">#FF58458B</Color>
    <Color x:Key="Color_0311">#FF211347</Color>
    <Color x:Key="Color_034">#FFEF1F3C</Color>

    <!-- ********************************** BRUSHES **********************************-->
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="LightForegroundBrush" Color="{StaticResource Color_009}"/>
    <SolidColorBrush x:Key="TransparentBrush" Color="{StaticResource Color_014}"/>
    <SolidColorBrush x:Key="LegendBgBrush" Color="{StaticResource Color_009}"/>
    <SolidColorBrush x:Key="LegendItemFill" Color="{StaticResource Color_009}"/>
    <SolidColorBrush x:Key="LegendStroke" Color="{StaticResource Color_004}"/>

    <LinearGradientBrush x:Key="LegendScaleFill">
        <GradientStop Color="{StaticResource Color_003}" Offset="0"/>
        <GradientStop Color="{StaticResource Color_002}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="LegendScaleFill2">
        <GradientStop Color="{StaticResource Color_005}" Offset="0"/>
        <GradientStop Color="{StaticResource Color_003}" Offset="1"/>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="PlotAreaBorderBrush" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="AxisStrokeBrush" Color="{StaticResource Color_004}"/>
    <SolidColorBrush x:Key="ChartBackground" Color="{StaticResource Color_014}"/>
    <SolidColorBrush x:Key="StripeBrush" Color="{StaticResource Color_015}"/>
    <SolidColorBrush x:Key="MinorStrokeBrush" Color="{StaticResource Color_004}"/>
    <SolidColorBrush x:Key="NegativeBrush" Color="{StaticResource Color_034}"/>
    <SolidColorBrush x:Key="OutlineBrush" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="TrendlineBaseBrush" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="ScrollbarNormalFill" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="ScrollbarHoverFill" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="ScrollbarPressedFill" Color="{StaticResource Color_011}"/>
    <SolidColorBrush x:Key="ScrollbarDisabledFill" Color="{StaticResource Color_005}"/>

    <!-- Zoombar -->
    <SolidColorBrush x:Key="ArrowNormalFill" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="ArrowHoverFill" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="ArrowPressedFill" Color="{StaticResource Color_011}"/>
    <SolidColorBrush x:Key="ArrowDisabledFill" Color="{StaticResource Color_005}"/>
    <SolidColorBrush x:Key="ThumbBackgroundBrush" Color="{StaticResource Color_016}"/>
    <SolidColorBrush x:Key="VerticalThumbBackgroundBrush" Color="{StaticResource Color_016}"/>
    <SolidColorBrush x:Key="HorizontalBaseBrush" Color="{StaticResource Color_014}"/>
    <SolidColorBrush x:Key="VerticalBaseBrush" Color="{StaticResource Color_014}"/>
    <SolidColorBrush x:Key="ScrollbarElementStroke" Color="{StaticResource Color_014}"/>
    <SolidColorBrush x:Key="VerticalScaleElementBrush" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="VerticalScaleBorderBrush" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="ScaleElementBrush" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="ScaleBorderBrush" Color="{StaticResource Color_003}"/>
    <SolidColorBrush x:Key="ScaleHoverBrush" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="ScalePressBrush" Color="{StaticResource Color_011}"/>
    <SolidColorBrush x:Key="ScaleHoverBrush_Vert" Color="{StaticResource Color_002}"/>
    <SolidColorBrush x:Key="ScalePressBrush_Vert" Color="{StaticResource Color_011}"/>
    <SolidColorBrush x:Key="ScaleResizerFill" Color="{StaticResource Color_009}"/>

    <!-- *********************************  ChartBrushes  ********************************* -->
    <shared:BrushCollection x:Key="ChartBrushes">
        <SolidColorBrush Color="{StaticResource Color_011}"/>
        <SolidColorBrush Color="{StaticResource Color_022}"/>
        <SolidColorBrush Color="{StaticResource Color_023}"/>
        <SolidColorBrush Color="{StaticResource Color_024}"/>
        <SolidColorBrush Color="{StaticResource Color_025}"/>
        <SolidColorBrush Color="{StaticResource Color_026}"/>
        <SolidColorBrush Color="{StaticResource Color_027}"/>
        <SolidColorBrush Color="{StaticResource Color_028}"/>
        <SolidColorBrush Color="{StaticResource Color_029}"/>
        <SolidColorBrush Color="{StaticResource Color_030}"/>
        <SolidColorBrush Color="{StaticResource Color_031}"/>
    </shared:BrushCollection>

    <!-- *********************************  ChartOutlines  ********************************* -->
    <shared:BrushCollection x:Key="ChartOutlines">
        <SolidColorBrush Color="{StaticResource Color_0111}"/>
        <SolidColorBrush Color="{StaticResource Color_0221}"/>
        <SolidColorBrush Color="{StaticResource Color_0231}"/>
        <SolidColorBrush Color="{StaticResource Color_0241}"/>
        <SolidColorBrush Color="{StaticResource Color_0251}"/>
        <SolidColorBrush Color="{StaticResource Color_0261}"/>
        <SolidColorBrush Color="{StaticResource Color_0271}"/>
        <SolidColorBrush Color="{StaticResource Color_0281}"/>
        <SolidColorBrush Color="{StaticResource Color_0291}"/>
        <SolidColorBrush Color="{StaticResource Color_0301}"/>
        <SolidColorBrush Color="{StaticResource Color_0311}"/>
    </shared:BrushCollection>

    <!-- ******************************************************** SL SPECIFIC INLINE-CONTROLS RESOURCES ***********************************************************
***********************************************************************************************************************************************************************    -->

    <!-- *********************************  ScrollBar PageButton Style  ********************************* -->
    <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton">
        <Setter Property="MinWidth" Value="30"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Grid x:Name="Root">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Arrow">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowHoverFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Arrow">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowPressedFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Arrow">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowDisabledFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Rectangle Fill="{StaticResource TransparentBrush}" Margin="-5"/>
                        <Path x:Name="Arrow" Data="F1 M 541.537,173.589L 531.107,173.589L 536.322,167.49L 541.537,173.589 Z " Height="6" Stretch="Uniform" Width="10" Fill="{StaticResource ArrowNormalFill}" Margin="{TemplateBinding Padding}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  Vertical Scrollbar Thumb Style  ********************************* -->
    <Style x:Key="ScrollbarVThumb" TargetType="Thumb">
        <Setter Property="MinWidth" Value="30"/>
        <Setter Property="MinHeight" Value="20"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Thumb">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowHoverFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Thumb">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowPressedFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Thumb">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowDisabledFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Rectangle Fill="{StaticResource TransparentBrush}" Margin="-5"/>
                        <Rectangle x:Name="Thumb" Fill="{StaticResource ScrollbarNormalFill}" Width="8"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  Horizontal Scrollbar Thumb Style  ********************************* -->
    <Style x:Key="ScrollbarHThumb" TargetType="Thumb">
        <Setter Property="MinWidth" Value="20"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Thumb">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowHoverFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Thumb">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowPressedFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0" Storyboard.TargetProperty="Fill" Storyboard.TargetName="Thumb">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ArrowDisabledFill}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Rectangle Fill="{StaticResource TransparentBrush}" Margin="-5"/>
                        <Rectangle x:Name="Thumb" Fill="{StaticResource ScrollbarNormalFill}" Height="8"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  Scrollbar Style  ********************************* -->
    <Style x:Key="ScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="MinWidth" Value="30"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid x:Name="Root">
                        <Grid.Resources>
                            <ControlTemplate x:Key="RepeatButtonTemplate" TargetType="RepeatButton">
                                <Grid x:Name="Root" Background="{StaticResource TransparentBrush}">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal"/>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Grid>
                            </ControlTemplate>
                        </Grid.Resources>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver"/>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0" To="0.5" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="Root"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="HorizontalRoot">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <RepeatButton x:Name="HorizontalSmallDecrease" Grid.Column="0" IsTabStop="False" Interval="50" Style="{StaticResource ScrollBarPageButton}" RenderTransformOrigin="0.5,0.5" VerticalAlignment="Center">
                                <RepeatButton.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform/>
                                        <SkewTransform/>
                                        <RotateTransform Angle="-90"/>
                                        <TranslateTransform/>
                                    </TransformGroup>
                                </RepeatButton.RenderTransform>
                            </RepeatButton>
                            <RepeatButton x:Name="HorizontalLargeDecrease" Grid.Column="1" IsTabStop="False" Interval="50" Template="{StaticResource RepeatButtonTemplate}" Width="0"/>
                            <Thumb x:Name="HorizontalThumb" Background="{TemplateBinding Background}" Grid.Column="2" MinWidth="18" Style="{StaticResource ScrollbarHThumb}" VerticalAlignment="Center"/>
                            <RepeatButton x:Name="HorizontalLargeIncrease" Grid.Column="3" IsTabStop="False" Interval="50" Template="{StaticResource RepeatButtonTemplate}"/>
                            <RepeatButton x:Name="HorizontalSmallIncrease" Grid.Column="4" IsTabStop="False" Interval="50" Style="{StaticResource ScrollBarPageButton}" RenderTransformOrigin="0.5,0.5" VerticalAlignment="Center">
                                <RepeatButton.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform/>
                                        <SkewTransform/>
                                        <RotateTransform Angle="90"/>
                                        <TranslateTransform/>
                                    </TransformGroup>
                                </RepeatButton.RenderTransform>
                            </RepeatButton>
                        </Grid>
                        <Grid x:Name="VerticalRoot" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <RepeatButton x:Name="VerticalSmallDecrease" IsTabStop="False" Interval="50" Grid.Row="0" Style="{StaticResource ScrollBarPageButton}" HorizontalAlignment="Center"/>
                            <RepeatButton x:Name="VerticalLargeDecrease" Height="0" IsTabStop="False" Interval="50" Grid.Row="1" Template="{StaticResource RepeatButtonTemplate}"/>
                            <Thumb x:Name="VerticalThumb" MinHeight="18" Grid.Row="2" Style="{StaticResource ScrollbarVThumb}" HorizontalAlignment="Center"/>
                            <RepeatButton x:Name="VerticalLargeIncrease" IsTabStop="False" Interval="50" Grid.Row="3" Template="{StaticResource RepeatButtonTemplate}"/>
                            <RepeatButton x:Name="VerticalSmallIncrease" IsTabStop="False" Interval="50" Grid.Row="4" Style="{StaticResource ScrollBarPageButton}" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Center">
                                <RepeatButton.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform/>
                                        <SkewTransform/>
                                        <RotateTransform Angle="180"/>
                                        <TranslateTransform/>
                                    </TransformGroup>
                                </RepeatButton.RenderTransform>
                            </RepeatButton>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  ScrollViewer Style ********************************* -->
    <Style x:Key="ScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="ScrollContentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="VerticalScrollBar" Grid.Column="1" IsTabStop="False" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Orientation="Vertical" Grid.Row="0" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{TemplateBinding VerticalOffset}" ViewportSize="{TemplateBinding ViewportHeight}" Width="18" Style="{StaticResource ScrollBarStyle}"/>
                            <ScrollBar x:Name="HorizontalScrollBar" Grid.Column="0" Height="18" IsTabStop="False" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{TemplateBinding HorizontalOffset}" ViewportSize="{TemplateBinding ViewportWidth}" Style="{StaticResource ScrollBarStyle}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ******************************************************** SL-WPF SHARED CONTROLS RESOURCES ***********************************************************
********************************************************************************************************************************************************************    -->

    <!-- ************************************************ Markers and MarkerTemplates ***********************************************-->
    <Style TargetType="local:Marker" x:Key="MarkerStyle">
        <Setter Property="Margin" Value="6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Marker">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <!-- <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Offset" Storyboard.TargetProperty="X" To="0.0" Duration="0:0:0.5"/>
                                        <DoubleAnimation Storyboard.TargetName="Offset" Storyboard.TargetProperty="Y" To="0.0" Duration="0:0:0.5"/>
                                    </Storyboard> -->
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                    <!-- <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Offset" Storyboard.TargetProperty="X" To="-4.0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="Offset" Storyboard.TargetProperty="Y" To="-4.0" Duration="0:0:0.2"/>
                                    </Storyboard> -->
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="6" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}">
                            <ContentPresenter.RenderTransform>
                                <TranslateTransform x:Name="Offset" X="0" Y="0"/>
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                        <!--
                        <Ellipse x:Name="Halo" IsHitTestVisible="False" Opacity="0" Stroke="{StaticResource PlotAreaBorderBrush}" StrokeThickness="1">
                            <Ellipse.Fill>
                                <RadialGradientBrush GradientOrigin="0.5, 0.5" Center="0.5, 0.5" RadiusX="0.5" RadiusY="0.5">
                                    <RadialGradientBrush.GradientStops>
                                        <GradientStop Color="#0098C1DC" Offset="0.0"/>
                                        <GradientStop Color="#2098C1DC" Offset="0.5"/>
                                        <GradientStop Color="#ff98C1DC" Offset="1"/>
                                    </RadialGradientBrush.GradientStops>
                                </RadialGradientBrush>
                            </Ellipse.Fill>
                        </Ellipse>
-->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:Marker" BasedOn="{StaticResource MarkerStyle}" />

    <DataTemplate x:Key="CircleMarkerTemplate">
        <Path Stretch="Fill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5">
            <Path.Data>
                <PathGeometry>
                    <PathGeometry.Figures>
                        <PathFigure StartPoint="0,0">
                            <PathFigure.Segments>
                                <ArcSegment Size="4,4" RotationAngle="0" IsLargeArc="True" SweepDirection="Clockwise" Point="0,1"/>
                                <ArcSegment Size="4,4" RotationAngle="0" IsLargeArc="False" SweepDirection="Clockwise" Point="0,0"/>
                            </PathFigure.Segments>
                        </PathFigure>
                    </PathGeometry.Figures>
                </PathGeometry>
            </Path.Data>
        </Path>
    </DataTemplate>
    <DataTemplate x:Key="TriangleMarkerTemplate">
        <Polygon Points="0, 0 4, 8 8, 0" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="PyramidMarkerTemplate">
        <Polygon Points="0, 8 4, 0 8, 8" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="SquareMarkerTemplate">
        <Polygon Points="0 0 0 8 8 8 8 0" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="DiamondMarkerTemplate">
        <Polygon Points="0 4 4 8 8 4 4 0" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="PentagonMarkerTemplate">
        <Polygon Points="1 0, 4 3, 7 0, 8 1, 5 4, 8 7, 7 8, 4 5, 1 8, 0 7, 3 4, 0 1" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="HexagonMarkerTemplate">
        <Polygon Points="3.2 0, 4.8 0, 4.8 3, 7 1, 8 2, 6 4, 8 6, 7 7, 4.8 5, 4.8 8, 3.2 8, 3.2 5, 1 7, 0 6, 2 4, 0 2, 1 1, 3.2 3" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="TetragramMarkerTemplate">
        <Polygon Points="4 0 2.5856 2.5856 0 4 2.5856 5.4144 4 8 5.4144 5.4144 8 4 5.4144 2.5856" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="PentagramMarkerTemplate">
        <Polygon Points="4 0 2.8244 2.382 0.1956 2.764 2.098 4.618 1.6488 7.236 4 6 6.3512 7.236 5.902 4.618 7.8044 2.764 5.1756 2.382" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>
    <DataTemplate x:Key="HexagramMarkerTemplate">
        <Polygon Points="4 0 3 2.268 0.536 2 2 4 0.536 6 3 5.732 4 8 5 5.732 7.464 6 6 4 7.464 2 5 2.268" Stretch="Fill" Fill="{Binding ActualItemBrush}" Stroke="{Binding Series.ActualMarkerOutline}" StrokeThickness="0.5"/>
    </DataTemplate>

    <!-- *********************************  Tooltip  ********************************* -->
    <Style x:Key="ToolTipStyle" TargetType="ContentControl">
        <Setter Property="Background" Value="{StaticResource LegendBgBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LegendStroke}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="FontSize" Value="14.667"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Padding" Value="5,7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Grid>
                        <Rectangle Stroke="{TemplateBinding BorderBrush}" Fill="{TemplateBinding Background}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                        <StackPanel Orientation="Horizontal" >
                            <ContentPresenter Content="{Binding}" ContentTemplate="{Binding Series.LegendItemBadgeTemplate}" Margin="7,0,0,0"/>
                            <ContentPresenter Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}"/>
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  CrosshairTooltipItemTemplate  ********************************* -->
    <DataTemplate x:Key="CrosshairTooltipItemTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentPresenter Content="{Binding ItemContext}" ContentTemplate="{Binding Series.LegendItemBadgeTemplate}" />
            <ContentPresenter Content="{Binding SubContent}" />
        </StackPanel>
    </DataTemplate>

    <!-- *********************************  ToolTipStyle NoBadge  ********************************* -->
    <Style x:Key="ToolTipStyleNoBadge" TargetType="ContentControl">
        <Setter Property="Background" Value="{StaticResource LegendBgBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LegendStroke}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="FontSize" Value="14.667"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Border>
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="{TemplateBinding Background}" >
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}"/>
                            </StackPanel>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  Legend Style  ********************************* -->
    <Style TargetType="local:Legend" x:Key="LegendStyle">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="FontSize" Value="14.667"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Background" Value="{StaticResource LegendBgBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LegendStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="Orientation" Value="Vertical"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Legend">
                    <Grid>
                        <Rectangle Fill="{StaticResource TransparentBrush}"/>
                        <Rectangle Stroke="{TemplateBinding BorderBrush}" Margin="0" Fill="{TemplateBinding Background}" StrokeThickness="{TemplateBinding BorderThickness}" Grid.Column="1"/>
                        <Border BorderThickness="{TemplateBinding BorderThickness}" Grid.Column="1">
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter Grid.Row="0" Content="{TemplateBinding Content}"/>
                                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="{StaticResource TransparentBrush}" Style="{StaticResource ScrollViewerStyle}">
                                    <ContentPresenter Grid.Row="1" x:Name="ContentPresenter"/>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:Legend" BasedOn="{StaticResource LegendStyle}" />

    <!-- ************************************************ Legend Item Templates **********************************************-->
    <!-- LegendItemContent -->
    <DataTemplate x:Key="LegendItemTemplate">
        <StackPanel Orientation="Horizontal" Margin="1" Visibility="{Binding Series.Visibility}">
            <ContentPresenter Content="{Binding}" ContentTemplate="{Binding Series.LegendItemBadgeTemplate}"/>
            <ContentPresenter Content="{Binding Series.Title}"/>
        </StackPanel>
    </DataTemplate>
    <!-- OverlayLegendItemTemplate -->
    <DataTemplate x:Key="OverlayLegendItemTemplate">
        <StackPanel Orientation="Horizontal" Margin="1" Visibility="{Binding Series.Visibility}">
            <ContentPresenter Content="{Binding}" ContentTemplate="{Binding Series.LegendItemBadgeTemplate}"/>
            <ContentPresenter Content="{Binding Series.Title}"/>
        </StackPanel>
    </DataTemplate>
    <!-- DiscreteLegendItemContent - used for ItemLegend -->
    <DataTemplate x:Key="DiscreteLegendItemTemplate">
        <StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Series.Visibility}">
            <ContentPresenter Content="{Binding}" ContentTemplate="{Binding Series.LegendItemBadgeTemplate}"/>
            <ContentPresenter Content="{Binding ItemLabel}"/>
        </StackPanel>
    </DataTemplate>
    <!-- PieChartLegendItem-->
    <DataTemplate x:Key="PieLegendItemTemplate">
        <StackPanel Orientation="Horizontal" Margin="1" Visibility="{Binding Series.Visibility}">
            <ContentPresenter Content="{Binding}" ContentTemplate="{Binding Series.LegendItemBadgeTemplate}"/>
            <ContentPresenter Content="{Binding ItemLabel}"/>
        </StackPanel>
    </DataTemplate>
    <!-- PieChartLegendItemBadge-->
    <DataTemplate x:Key="PieChartLegendItemBadgeTemplate">
        <ContentPresenter Width="11" Height="11" Margin="0 0 2 0" VerticalAlignment="Center" HorizontalAlignment="Center" Content="{Binding}">
            <ContentPresenter.ContentTemplate>
                <DataTemplate>
                    <Rectangle Stroke="{Binding Slice.BorderBrush}" Fill="{Binding Slice.Background}" StrokeThickness="{Binding Slice.StrokeThickness}" />
                </DataTemplate>
            </ContentPresenter.ContentTemplate>
        </ContentPresenter>
    </DataTemplate>
    <!-- FunnelChartLegendItemBadge-->
    <DataTemplate x:Key="FunnelChartLegendItemBadgeTemplate">
        <ContentPresenter Width="11" Height="11" Margin="0 0 2 0" VerticalAlignment="Center" HorizontalAlignment="Center" Content="{Binding}">
            <ContentPresenter.ContentTemplate>
                <DataTemplate>
                    <Rectangle Stroke="{Binding ItemOutline}" Fill="{Binding ItemBrush}"/>
                </DataTemplate>
            </ContentPresenter.ContentTemplate>
        </ContentPresenter>
    </DataTemplate>
    <!-- LegendItem -->
    <DataTemplate x:Key="LegendItemRectBadgeTemplate">
        <Grid Width="19" Height="13" Margin="0,0,5,0">
            <Rectangle VerticalAlignment="Center" HorizontalAlignment="Center"  Fill="{Binding Series.ActualBrush}" Stroke="{Binding Series.ActualOutline}" StrokeLineJoin="Round" StrokeThickness="{Binding Series.Thickness}" Width="13" Height="13"/>
            <ContentPresenter Width="11" Height="11" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0" ContentTemplate="{Binding Series.ActualMarkerTemplate}" Content="{Binding}"/>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemMarkerlessRectBadgeTemplate">
        <Grid Width="19" Height="13" Margin="0,0,5,0">
            <Rectangle VerticalAlignment="Center" HorizontalAlignment="Center"  Fill="{Binding Series.ActualBrush}" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}" Width="13" Height="13" StrokeLineJoin="Round"/>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemLineBadgeTemplate">
        <Grid Width="19" Height="13" Margin="0,0,5,0">
            <Rectangle  VerticalAlignment="Center" StrokeThickness="3" Fill="{Binding Series.ActualBrush}" Height="3" RadiusX="1" RadiusY="1"/>
            <ContentPresenter Width="13" Height="13" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0" ContentTemplate="{Binding Series.ActualMarkerTemplate}" Content="{Binding}"/>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemMarkerlessLineBadgeTemplate">
        <Grid Width="18" Height="16" Margin="0 0 2 0">
            <Line VerticalAlignment="Center" HorizontalAlignment="Center" X1="0" Y1="0" X2="16" Y2="0" Stroke="{Binding Series.ActualBrush}" StrokeThickness="{Binding Series.Thickness}" StrokeEndLineCap="{Binding Series.EndCap}" StrokeStartLineCap="{Binding Series.StartCap}" />
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemPointBadgeTemplate">
        <ContentPresenter Width="13" Height="13" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,5,0" ContentTemplate="{Binding Series.ActualMarkerTemplate}" Content="{Binding}"/>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemFinancialBadgeTemplate">
        <Grid Width="19" Height="13" Margin="0,0,5,0">
            <Grid Width="13" Height="13">
                <Rectangle Fill="{StaticResource LegendItemFill}" Width="13" Height="13" Margin="0"/>
                <Path x:Name="path" Height="12" Width="13" Stretch="Fill" Opacity="1" Data="M176,109 C176,109 206,79 206,79 C206,79 176,79 176,79 C176,79 176,109 176,109 z"  Fill="{Binding Series.ActualBrush}" HorizontalAlignment="Left" StrokeLineJoin="Round" VerticalAlignment="Top" UseLayoutRounding="False" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}"/>
                <Path x:Name="path1" Height="12" Width="13" Stretch="Fill" Opacity="1" Data="M176,109 C176,109 206,109 206,109 C206,109 206,79 206,79 C206,79 176,109 176,109 z"  Fill="{Binding Series.NegativeBrush}" Margin="0" UseLayoutRounding="False" StrokeLineJoin="Round" HorizontalAlignment="Right" VerticalAlignment="Bottom" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}"/>
            </Grid>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemFinancialIndicatorBadgeTemplate">
        <Grid Width="19" Height="13" Margin="0,0,5,0">
            <Grid Width="13" Height="13">
                <Rectangle Fill="{StaticResource LegendItemFill}" Width="13" Height="13" Margin="0"/>
                <Path x:Name="path" Height="12" Width="13" Stretch="Fill" Opacity="1" Data="M176,109 C176,109 206,79 206,79 C206,79 176,79 176,79 C176,79 176,109 176,109 z"  Fill="{Binding Series.ActualBrush}" HorizontalAlignment="Left" VerticalAlignment="Top" StrokeLineJoin="Round" UseLayoutRounding="False" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}"/>
                <Path x:Name="path1" Height="12" Width="13" Stretch="Fill" Opacity="1" Data="M176,109 C176,109 206,109 206,109 C206,109 206,79 206,79 C206,79 176,109 176,109 z"  Fill="{Binding Series.NegativeBrush}" StrokeLineJoin="Round" Margin="0" UseLayoutRounding="False" HorizontalAlignment="Right" VerticalAlignment="Bottom" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}"/>
            </Grid>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="LegendItemPositiveNegativeBadgeTemplate">
        <Grid Width="19" Height="13" Margin="0,0,5,0">
            <Grid Width="13" Height="13">
                <Rectangle Fill="{StaticResource LegendItemFill}" Width="13" Height="13" Margin="0"/>
                <Path x:Name="path" Height="12" Width="13" Stretch="Fill" Opacity="1" Data="M176,109 C176,109 206,79 206,79 C206,79 176,79 176,79 C176,79 176,109 176,109 z"  Fill="{Binding Series.ActualBrush}" HorizontalAlignment="Left" VerticalAlignment="Top" StrokeLineJoin="Round" UseLayoutRounding="False" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}"/>
                <Path x:Name="path1" Height="12" Width="13" Stretch="Fill" Opacity="1" Data="M176,109 C176,109 206,109 206,109 C206,109 206,79 206,79 C206,79 176,109 176,109 z"  Fill="{Binding Series.NegativeBrush}" Margin="0" StrokeLineJoin="Round" UseLayoutRounding="False" HorizontalAlignment="Right" VerticalAlignment="Bottom" Stroke="{Binding Series.ActualOutline}" StrokeThickness="{Binding Series.Thickness}"/>
            </Grid>
            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" ContentTemplate="{Binding Series.ActualMarkerTemplate}" Content="{Binding}"/>
        </Grid>
    </DataTemplate>

    <!-- *********************************  ItemLegend style  ********************************* -->
    <Style TargetType="local:ItemLegend" x:Key="ItemLegendStyle">
        <Style.Setters>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="FontSize" Value="14.667"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="{StaticResource LegendBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LegendStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:ItemLegend">
                        <Grid>
                            <Rectangle Fill="{StaticResource TransparentBrush}"/>
                            <Rectangle Stroke="{TemplateBinding BorderBrush}" Margin="0" Fill="{TemplateBinding Background}" StrokeThickness="{TemplateBinding BorderThickness}" Grid.Column="1"/>
                            <Border BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0" Content="{TemplateBinding Content}"/>
                                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" BorderThickness="0" Style="{StaticResource ScrollViewerStyle}">
                                        <ContentPresenter Grid.Row="1" x:Name="ContentPresenter"/>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
    <Style TargetType="local:ItemLegend" BasedOn="{StaticResource ItemLegendStyle}" />

    <!-- *********************************  ScaleLegendTemplate  ********************************* -->
    <DataTemplate x:Key="ScaleLegendTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Polygon x:Name="LegendScale" Points="3 0, 5 0, 5 1, 0 1, 3 0" Stretch="Fill" Grid.Column="0" Margin="2,2,5,2" Fill="{StaticResource LegendScaleFill}"/>
            <TextBlock Text="Min" Grid.Column="1" VerticalAlignment="Top"/>
            <TextBlock Text="Max" Grid.Column="1" VerticalAlignment="Bottom"/>
        </Grid>
    </DataTemplate>

    <!-- *********************************  ScaleLegend style  ********************************* -->
    <Style TargetType="local:ScaleLegend" x:Key="ScaleLegendStyle">
        <Style.Setters>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="FontSize" Value="14.667"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="{StaticResource LegendBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LegendStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="MinHeight" Value="50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:ScaleLegend">
                        <Grid>
                            <Rectangle Fill="{StaticResource TransparentBrush}"/>
                            <Rectangle Stroke="{TemplateBinding BorderBrush}" Margin="0" Fill="{TemplateBinding Background}" StrokeThickness="{TemplateBinding BorderThickness}" Grid.Column="1"/>
                            <Border BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0" Content="{TemplateBinding Content}"/>
                                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" BorderThickness="0" Style="{StaticResource ScrollViewerStyle}">
                                        <ContentPresenter Grid.Row="1" x:Name="ContentPresenter">
                                            <ContentPresenter.Content>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition/>
                                                    </Grid.ColumnDefinitions>
                                                    <Polygon x:Name="LegendScale" Points="3 0, 5 0, 5 1, 0 1, 3 0" Stretch="Fill" Grid.Column="0" Margin="2,2,5,2" Fill="{StaticResource LegendScaleFill2}"/>
                                                    <TextBlock x:Name="MinText" Text="Min" Grid.Column="1" VerticalAlignment="Top"/>
                                                    <TextBlock x:Name="MaxText" Text="Max" Grid.Column="1" VerticalAlignment="Bottom"/>
                                                </Grid>
                                            </ContentPresenter.Content>
                                        </ContentPresenter>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
    <Style TargetType="local:ScaleLegend" BasedOn="{StaticResource ScaleLegendStyle}" />
    <!--
    <DataTemplate x:Key="LegendItemColorTemplate">
        <local:ColorScaleLegendItem Width="100"
                                    Height="50"
                                    Scale="{Binding Series.Scale}" />
    </DataTemplate>

    <Style TargetType="local:ColorScaleLegendItem" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ColorScaleLegendItem">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ContentPresenter x:Name="ContentPresenter" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style> -->

    <!-- *********************************  Chart preview path style  ********************************* -->
    <Style x:Key="PreviewPathStyle" TargetType="Path">
        <Setter Property="Fill" Value="{StaticResource PlotAreaBorderBrush}"/>
        <Setter Property="Opacity" Value="0.5"/>
    </Style>

    <!-- *********************************  Chart crosshair line style  ********************************* -->
    <Style x:Key="CrosshairLineStyle" TargetType="Line">
        <Setter Property="Stroke" Value="{StaticResource PlotAreaBorderBrush}"/>
        <Setter Property="StrokeThickness" Value="1.4"/>
        
    </Style>

    <!-- *********************************  Chart style  ********************************* -->
    <Style TargetType="local:XamDataChart" x:Key="XamDataChartStyle">
        <Setter Property="ZoombarStyle">
            <Setter.Value>
                <Style TargetType="ig:XamZoombar" >
                    <Setter Property="Margin" Value="8" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Brushes" Value="{StaticResource ChartBrushes}"/>
        <Setter Property="MarkerBrushes" Value="{StaticResource ChartBrushes}"/>
        <Setter Property="Outlines" Value="{StaticResource ChartOutlines}"/>
        <Setter Property="MarkerOutlines" Value="{StaticResource ChartOutlines}"/>
        <Setter Property="CircleMarkerTemplate" Value="{StaticResource CircleMarkerTemplate}"/>
        <Setter Property="TriangleMarkerTemplate" Value="{StaticResource TriangleMarkerTemplate}"/>
        <Setter Property="PyramidMarkerTemplate" Value="{StaticResource PyramidMarkerTemplate}"/>
        <Setter Property="SquareMarkerTemplate" Value="{StaticResource SquareMarkerTemplate}"/>
        <Setter Property="DiamondMarkerTemplate" Value="{StaticResource DiamondMarkerTemplate}"/>
        <Setter Property="PentagonMarkerTemplate" Value="{StaticResource PentagonMarkerTemplate}"/>
        <Setter Property="HexagonMarkerTemplate" Value="{StaticResource HexagonMarkerTemplate}"/>
        <Setter Property="TetragramMarkerTemplate" Value="{StaticResource TetragramMarkerTemplate}"/>
        <Setter Property="PentagramMarkerTemplate" Value="{StaticResource PentagramMarkerTemplate}"/>
        <Setter Property="HexagramMarkerTemplate" Value="{StaticResource HexagramMarkerTemplate}"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="PlotAreaMinWidth" Value="50"/>
        <Setter Property="PlotAreaMinHeight" Value="50"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="PlotAreaBackground" Value="{StaticResource ChartBackground}"/>
        <Setter Property="PlotAreaBorderBrush" Value="{StaticResource PlotAreaBorderBrush}"/>
        <Setter Property="PlotAreaBorderThickness" Value="0.5"/>
        <Setter Property="PreviewPathStyle" Value="{StaticResource PreviewPathStyle}"/>
        <Setter Property="CrosshairLineStyle" Value="{StaticResource CrosshairLineStyle}"/>
        <Setter Property="CrosshairVisibility" Value="Collapsed"/>
        <Setter Property="ToolTipStyle" Value="{StaticResource ToolTipStyle}"/>
        <Setter Property="CrosshairTooltipStyle" Value="{StaticResource ToolTipStyleNoBadge}" />
        <Setter Property="CrosshairTooltipItemTemplate" Value="{StaticResource CrosshairTooltipItemTemplate}" />
        <Setter Property="OverviewPlusDetailPaneStyle">
            <Setter.Value>
                <Style TargetType="ig:XamOverviewPlusDetailPane" >
                    <Setter Property="ZoomTo100ButtonVisibility" Value="Collapsed" />
                    <Setter Property="InteractionStatesToolVisibility" Value="Visible" />
                    <Setter Property="ScaleToFitButtonToolTip" Value="Reset Zoom (100%)" />
                    <Setter Property="ZoomLevelLargeChange" Value="0.2" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:XamDataChart">
                    <Grid x:Name="RootLayout" UseLayoutRounding="False">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="MouseStates">
                                <VisualState x:Name="Idle">
                                </VisualState>
                                <VisualState x:Name="Dragging">
                                </VisualState>
                                <VisualState x:Name="Panning">
                                </VisualState>
                                <VisualState x:Name="Inking">
                                </VisualState>
                                <VisualState x:Name="Erasing">
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="4" Background="{TemplateBinding Background}">
                            <Grid Margin="2">
                                <ContentPresenter x:Name="ContentPresenter" Margin="{TemplateBinding Padding}"/>
                                <Rectangle x:Name="Overlay" IsHitTestVisible="False">
                                    <Rectangle.Fill>
                                        <ImageBrush/>
                                    </Rectangle.Fill>
                                </Rectangle>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:XamDataChart" BasedOn="{StaticResource XamDataChartStyle}" />

    <!-- ************************************************ AXIS ************************************************ -->
    <Style TargetType="local:Axis">
        <Setter Property="Stroke" Value="{StaticResource PlotAreaBorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="StrokeThickness" Value="1"/>
        <!-- <Setter Property="StrokeDashArray" Value="1 2 3 4"/> -->
        <!--<Setter Property="Strip" Value="{StaticResource StripeBrush}" />-->
        <Setter Property="MajorStroke" Value="{StaticResource AxisStrokeBrush}"/>
        <Setter Property="MajorStrokeThickness" Value="0.70"/>
        <!-- <Setter Property="MajorStrokeDashArray" Value="1 2 3 4"/> -->
        <!-- <Setter Property="MinorStroke" Value="{StaticResource MinorStrokeBrush}" /> -->
        <Setter Property="MinorStrokeThickness" Value="0.70"/>
        <!-- <Setter Property="MinorStrokeDashArray" Value="1 2 3 4"/> -->
        <Setter Property="IsInverted" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Axis">
                    <Canvas x:Name="RootCanvas"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<Style TargetType="local:CategoryXAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:CategoryYAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:NumericXAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:NumericYAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:CategoryAngleAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:NumericAngleAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:NumericRadiusAxis" BasedOn="{StaticResource Axis}"/>
    <Style TargetType="local:CategoryDateTimeXAxis" BasedOn="{StaticResource Axis}"/>-->

    <!-- ********************************** SERIES ********************************** -->
    <Style x:Key="Series" TargetType="local:Series">
        <Setter Property="MinWidth" Value="8"/>
        <Setter Property="Thickness" Value="1"/>
        <Setter Property="MinHeight" Value="8"/>
        <!-- do not set a default value for Outline, or else the XamDataChart.Outlines property will be ineffective. -->
        <Setter Property="LegendItemTemplate" Value="{StaticResource LegendItemTemplate}"/>
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Series">
                    <Canvas x:Name="RootCanvas">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <!-- <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootCanvas" Storyboard.TargetProperty="Opacity" To="0.9" Duration="0:0:0.4"/>
                                    </Storyboard> -->
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                    <!-- <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootCanvas" Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.2"/>
                                    </Storyboard> -->
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:ColumnSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:BarSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:WaterfallSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemPositiveNegativeBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="Outline" Value="{StaticResource OutlineBrush}"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:RangeColumnSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
    </Style>
    <Style TargetType="local:StepLineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:LineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:SplineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:StepAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="0"/>
    </Style>
    <Style TargetType="local:AreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="0"/>
    </Style>
    <Style TargetType="local:SplineAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="0"/>
    </Style>
    <Style TargetType="local:RangeAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
    </Style>
    <Style TargetType="local:PointSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemPointBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>

    <!-- *********************************  STACKED SERIES  ********************************* -->
    <Style TargetType="local:StackedColumnSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:StackedBarSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:Stacked100ColumnSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:Stacked100BarSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:StackedLineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:Stacked100LineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:StackedSplineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:Stacked100SplineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:StackedAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:StackedSplineAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:Stacked100AreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:Stacked100SplineAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>

    <!-- *********************************  SCATTERLINE  ********************************* -->
    <Style TargetType="local:ScatterSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemPointBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Series">
                    <Canvas x:Name="RootCanvas">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:HighDensityScatterSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemPointBadgeTemplate}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Series">
                    <Canvas x:Name="RootCanvas">
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:ScatterLineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="Resolution" Value="5"/>
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Series">
                    <Canvas x:Name="RootCanvas">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:ScatterSplineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="Resolution" Value="5"/>
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Series">
                    <Canvas x:Name="RootCanvas">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  Bubble Series  ********************************* -->
    <Style TargetType="local:BubbleSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemPointBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
        <Setter Property="LegendItemTemplate" Value="{StaticResource LegendItemTemplate}"/>
        <Setter Property="DiscreteLegendItemTemplate" Value="{StaticResource DiscreteLegendItemTemplate}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Series">
                    <Canvas x:Name="RootCanvas">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- *********************************  FinancialPriceSeries  ********************************* -->
    <Style TargetType="local:FinancialPriceSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialBadgeTemplate}"/>
        <Setter Property="Resolution" Value="2"/>
        <Setter Property="DisplayType" Value="OHLC"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="Outline" Value="{StaticResource OutlineBrush}"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:BollingerBandsOverlay" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemMarkerlessRectBadgeTemplate}"/>
        <Setter Property="DashArray" Value="2 4 8 4"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:PriceChannelOverlay" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemMarkerlessRectBadgeTemplate}"/>
        <Setter Property="Thickness" Value="1"/>
    </Style>
    <Style TargetType="local:AbsoluteVolumeOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:AccumulationDistributionIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:AverageTrueRangeIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:CommodityChannelIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:ForceIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Area"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:MoneyFlowIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Area"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:MovingAverageConvergenceDivergenceIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Area"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="TrendLineBrush" Value="{StaticResource TrendlineBaseBrush}"/>
        <Setter Property="TrendLineType" Value="ExponentialAverage"/>
        <Setter Property="TrendLinePeriod" Value="9"/>
    </Style>
    <Style TargetType="local:OnBalanceVolumeIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:PercentagePriceOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:PercentageVolumeOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:PriceVolumeTrendIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:RateOfChangeAndMomentumIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:TypicalPriceIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:MarketFacilitationIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Area"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:RelativeStrengthIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Area"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:WeightedCloseIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:MedianPriceIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:DetrendedPriceOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:EaseOfMovementIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:MassIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:NegativeVolumeIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:PositiveVolumeIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:AverageDirectionalIndexIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:BollingerBandWidthIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:ChaikinVolatilityIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:ChaikinOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:StandardDeviationIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:TRIXIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="TrendLineBrush" Value="{StaticResource TrendlineBaseBrush}"/>
        <Setter Property="TrendLineType" Value="SimpleAverage"/>
        <Setter Property="TrendLinePeriod" Value="9"/>
    </Style>
    <Style TargetType="local:UltimateOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:WilliamsPercentRIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:FastStochasticOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="TrendLineBrush" Value="{StaticResource TrendlineBaseBrush}"/>
        <Setter Property="TrendLineType" Value="ExponentialAverage"/>
        <Setter Property="TrendLinePeriod" Value="3"/>
    </Style>
    <Style TargetType="local:SlowStochasticOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="TrendLineBrush" Value="{StaticResource TrendlineBaseBrush}"/>
        <Setter Property="TrendLineType" Value="ExponentialAverage"/>
        <Setter Property="TrendLinePeriod" Value="3"/>
    </Style>
    <Style TargetType="local:FullStochasticOscillatorIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
        <Setter Property="TrendLineBrush" Value="{StaticResource TrendlineBaseBrush}"/>
        <Setter Property="TrendLineType" Value="ExponentialAverage"/>
        <Setter Property="TrendLinePeriod" Value="3"/>
    </Style>
    <Style TargetType="local:CustomIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:StochRSIIndicator" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemFinancialIndicatorBadgeTemplate}"/>
        <Setter Property="DisplayType" Value="Line"/>
        <Setter Property="NegativeBrush" Value="{StaticResource NegativeBrush}"/>
    </Style>
    <Style TargetType="local:PolarLineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:PolarSplineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:PolarScatterSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemPointBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:PolarAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:RadialLineSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemLineBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:PolarSplineAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="Automatic"/>
    </Style>
    <Style TargetType="local:RadialAreaSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Thickness" Value="0"/>
    </Style>
    <Style TargetType="local:RadialColumnSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
    </Style>
    <Style TargetType="local:RadialPieSeries" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemRectBadgeTemplate}"/>
        <Setter Property="MarkerType" Value="None"/>
        <Setter Property="Resolution" Value="4"/>
    </Style>
    <Style TargetType="local:ValueOverlay" BasedOn="{StaticResource Series}">
        <Setter Property="LegendItemTemplate" Value="{StaticResource OverlayLegendItemTemplate}"/>
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource LegendItemMarkerlessLineBadgeTemplate}"/>
    </Style>

    <!-- *********************************  Pie Chart  ********************************* -->
    <Style TargetType="local:PieChartBase" x:Key="PieChartBase">
        <Setter Property="Brushes" Value="{StaticResource ChartBrushes}"/>
        <Setter Property="ToolTipStyle" Value="{StaticResource ToolTipStyle}"/>
        <Setter Property="LegendItemTemplate" Value="{StaticResource PieLegendItemTemplate}"/>
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource PieChartLegendItemBadgeTemplate}"/>
        <Setter Property="MinWidth" Value="50"/>
        <Setter Property="MinHeight" Value="50"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:PieChartBase">
                    <Grid Name="RootLayout" UseLayoutRounding="False">
                        <Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="4" Background="{TemplateBinding Background}">
                            <Grid Margin="2">
                                <ContentPresenter x:Name="ContentPresenter" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:XamPieChart" BasedOn="{StaticResource PieChartBase}">
    </Style>

    <!-- *********************************  Funnel Chart  ********************************* -->
    <Style TargetType="local:XamFunnelChart" x:Key="XamFunnelChart">
        <Setter Property="Brushes" Value="{StaticResource ChartBrushes}"/>
        <Setter Property="Outlines" Value="{StaticResource ChartOutlines}"/>
        <Setter Property="MinWidth" Value="200"/>
        <Setter Property="MinHeight" Value="200"/>
        <Setter Property="LegendItemTemplate" Value="{StaticResource DiscreteLegendItemTemplate}"/>
        <Setter Property="LegendItemBadgeTemplate" Value="{StaticResource FunnelChartLegendItemBadgeTemplate}"/>
        <Setter Property="SelectedSliceStyle">
            <Setter.Value>
                <Style TargetType="local:XamFunnelSlice">
                    <Setter Property="StrokeThickness" Value="3"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="UnselectedSliceStyle">
            <Setter.Value>
                <Style TargetType="local:XamFunnelSlice">
                    <Setter Property="Opacity" Value=".4"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:XamFunnelChart">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <local:XamFunnelView x:Name="PART_VIEW"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:XamFunnelChart" BasedOn="{StaticResource XamFunnelChart}" />

    <Style TargetType="local:XamFunnelView" x:Key="XamFunnelView">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:XamFunnelView">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid x:Name="PART_ROOT">
                            <Grid x:Name="PART_PLOTAREA" Background="{StaticResource TransparentBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid x:Name="PART_LEFTPANEL" Grid.Column="0">
                                    <Canvas x:Name="PART_LEFTOUTERLABELS"/>
                                </Grid>
                                <Canvas x:Name="PART_SLICEAREA" Grid.Column="1"/>
                                <Grid x:Name="PART_RIGHTPANEL" Grid.Column="2">
                                    <Canvas x:Name="PART_RIGHTOUTERLABELS"/>
                                </Grid>
                                <Popup x:Name="PART_TOOLTIP" IsOpen="False">
                                    <Border BorderBrush="{StaticResource LegendStroke}" BorderThickness="1" Background="{StaticResource LegendBgBrush}" IsHitTestVisible="False" Padding="3">
                                        <ContentControl x:Name="PART_TOOLTIPCONTENT"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:XamFunnelView" BasedOn="{StaticResource XamFunnelView}" />

    <Style TargetType="local:XamFunnelSlice" x:Key="XamFunnelSlice">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:XamFunnelSlice">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Polygon Points="{Binding Points}" Fill="{TemplateBinding ActualFill}" Stroke="{TemplateBinding ActualOutline}" StrokeThickness="{TemplateBinding StrokeThickness}"/>
                            <Canvas>
                                <ContentPresenter x:Name="PART_LABEL" Visibility="{TemplateBinding LabelVisibility}">
                                </ContentPresenter>
                            </Canvas>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:XamFunnelSlice" BasedOn="{StaticResource XamFunnelSlice}" />
</ResourceDictionary>
